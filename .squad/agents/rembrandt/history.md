# Rembrandt — History

## Core Context
Lead on Landgrab — a real-time multiplayer hex territory game. Stack: React 19 + TypeScript frontend, ASP.NET Core 8 + SignalR backend, SQL Server/PostgreSQL, Azure hosting. Owner: Léon van de Broek.

Key patterns:
- GameService is the main façade; domain services are singletons holding in-memory game state
- SignalR hub split into 4 partial classes (base, lobby, gameplay, host)
- Frontend uses Zustand stores; no external state library beyond that
- Server is the source of truth — frontend never mutates state locally
- JWT claims from both ClaimTypes.NameIdentifier and JwtRegisteredClaimNames.Sub
- Hex coordinates: axial (Q, R), grid key format "q,r"

## Learnings
- **2026-03-23 (game-manual-review-3):** Third review pass. De Ruyter's two fixes verified correct: (1) Commando Raid success condition at line 312 matches code; (2) Demolish step-by-step at lines 398–404 matches code. All other abilities, troop regen, win conditions, and turn structure confirmed accurate. HOWEVER: De Ruyter only fixed the detailed Demolish guide, not the Engineer role overview summary at line 231, which still says "Targets an enemy fort you're adjacent to." Code requires you to be ON the fort. REJECTED again. One remaining fix: line 231 "adjacent to" → "standing on". Locked-out: Erasmus, Vondel, De Ruyter.
- **2026-03-23 (game-manual-review-2):** Second review pass of `docs/game-manual.md` after Vondel applied corrections. All 7 of Vondel's fixes verified correct. Found 2 new critical errors missed in round 1: (1) Commando Raid success condition — manual says "≥50% of alliance members present" but code (`GameplayService.cs:680`) requires `attackers.Count >= 2 && attackers.Count > defenders.Count` (minimum 2 + must outnumber defenders); (2) Demolish initiation — manual says "stand adjacent to an enemy fort" to start, but `AbilityService.cs:745-752` checks `currentCell.IsFort` (your current hex must BE the fort). REJECTED. Verdict written to `.squad/decisions/inbox/rembrandt-manual-review-2.md`. Third agent (not Erasmus, not Vondel) assigned.
- **2026-03-23 (game-manual-review):** Reviewed `docs/game-manual.md` written by Erasmus. REJECTED. Found 5 critical factual errors verified against backend source: (1) FFA combat describes "advantage" (roll-twice) but code does 1d6+1 conditional on troop majority; (2) FFA ties go to DEFENDER (`>` not `>=`) but manual says attacker wins ties; (3) Fog of War is NOT optional — no `FogEnabled` flag exists in `GameDynamics`, always-on; (4) HQ claim freeze runs full 5 min regardless of recapture — code has no path to clear `ClaimFrozenUntil` early; (5) HQ hexes immune to direct combat (Commando Raid only) — manual says "recapture like any other hex" which is wrong. Two minor issues: enemy sighting memory UI max is 120s not 300s; Elimination tiebreaker framing misleading (should say alphabetical). Verdict written to `.squad/decisions/inbox/rembrandt-manual-review.md`.
- Team hired 2026-03-22 by Léon van de Broek
- Dutch Golden Age universe
- **2026-03-23 (beacon-redesign):** Led Scout beacon redesign architecture. Beacon changes from manual toggle to passive role trait (always-on when Scout has valid GPS). Share Intel becomes explicit 60s-cooldown ability for broadcasting beacon intel to alliance. Auto-activation hook: `GameplayService.UpdatePlayerLocation` (right place because beacon already requires location, and existing code maintains beacon fields when `IsBeacon` is true). Guards on manual toggle prevent Scouts from deactivating beacon. Server-side cone computation via `VisibilityService.ComputeBeaconSectorKeys` eliminates client drift. All role-specific logic gated on `PlayerRolesEnabled`; non-role games fully backward compatible. De Ruyter implemented backend changes (dotnet build + test ✅), Vermeer implemented frontend (npm build ✅) with concurrent rendering fixes (invisible cone tiles, `?` badge, instant reveal UX, pixel radius corrections). Merged decision #23 into `.squad/decisions.md`.
- **2026-03-23 (visibility-rearchitecture):** Investigated visibility architecture end-to-end. Key findings: (1) Backend does NOT strip raw tile data from Hidden cells — only stamps `VisibilityTier`. Frontend already has all data needed. (2) `BroadcastState` fires on EVERY hex change due to `movedToDifferentHex` check + Shepherd `LastVisitedAt` setting `gridChanged=true`. (3) This means per-player visibility recomputation (O(N×M)) runs on every movement. (4) The beacon fix already proved client-side visibility derivation works (`computeBeaconCone` in `beaconCone.ts`). Architecture decision: generalize the beacon pattern — frontend derives visibility locally from allied positions + owned territory + grid data. Backend reduces unnecessary full broadcasts. Deployed decision #25 into `.squad/decisions.md`. Coordinated De Ruyter (backend optimization) and Vermeer (frontend visibility derivation).
- **2026-03-24 (abilities-expansion-blueprint):** Produced full implementation blueprint for four ability changes: (1) Commando Raid — target changes to player's CURRENT hex; `ActivateCommandoRaid` loses both coord params; `ResolveRaidTarget` becomes a trivial current-hex stub for backward compat. (2) Tactical Strike — change `HexDistance > 1` check to `HexDistance != 1`; remove "Use Current Hex" frontend fallback. (3) Troop Transfer (NEW) — role-agnostic; bearing-based ally targeting with 45° cone tolerance; initiator/recipient SignalR events; `TroopTransferCooldownUntil` on `PlayerDto`; `ActiveTroopTransfers` on `GameState`. (4) Field Battle (NEW) — role-agnostic; neutral tile with enemy troops; `FieldBattleResolutionMode` enum (4 variants) in `GameDynamics`; 30-second join window via `Task.Run` + captured `IHubContext<GameHub>`; `battle.Resolved` flag prevents race between join and timer. Key arch decisions: Tactical Strike confirm required (no auto-fire); Troop Transfer shows name preview + confirm; Field Battle join via notification banner (not modal); no-join outcome = cancellation not win. Blueprint: `.squad/decisions/inbox/rembrandt-abilities-expansion-blueprint.md`. Decisions: `.squad/decisions/inbox/rembrandt-abilities-arch-decision.md`.

