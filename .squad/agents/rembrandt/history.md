# Rembrandt — History

## Core Context
Lead on Landgrab — a real-time multiplayer hex territory game. Stack: React 19 + TypeScript frontend, ASP.NET Core 8 + SignalR backend, SQL Server/PostgreSQL, Azure hosting. Owner: Léon van de Broek.

Key patterns:
- GameService is the main façade; domain services are singletons holding in-memory game state
- SignalR hub split into 4 partial classes (base, lobby, gameplay, host)
- Frontend uses Zustand stores; no external state library beyond that
- Server is the source of truth — frontend never mutates state locally
- JWT claims from both ClaimTypes.NameIdentifier and JwtRegisteredClaimNames.Sub
- Hex coordinates: axial (Q, R), grid key format "q,r"

## Learnings
- **2026-03-23 (game-manual-review-3):** Third review pass. De Ruyter's two fixes verified correct: (1) Commando Raid success condition at line 312 matches code; (2) Demolish step-by-step at lines 398–404 matches code. All other abilities, troop regen, win conditions, and turn structure confirmed accurate. HOWEVER: De Ruyter only fixed the detailed Demolish guide, not the Engineer role overview summary at line 231, which still says "Targets an enemy fort you're adjacent to." Code requires you to be ON the fort. REJECTED again. One remaining fix: line 231 "adjacent to" → "standing on". Locked-out: Erasmus, Vondel, De Ruyter.
- **2026-03-23 (game-manual-review-2):** Second review pass of `docs/game-manual.md` after Vondel applied corrections. All 7 of Vondel's fixes verified correct. Found 2 new critical errors missed in round 1: (1) Commando Raid success condition — manual says "≥50% of alliance members present" but code (`GameplayService.cs:680`) requires `attackers.Count >= 2 && attackers.Count > defenders.Count` (minimum 2 + must outnumber defenders); (2) Demolish initiation — manual says "stand adjacent to an enemy fort" to start, but `AbilityService.cs:745-752` checks `currentCell.IsFort` (your current hex must BE the fort). REJECTED. Verdict written to `.squad/decisions/inbox/rembrandt-manual-review-2.md`. Third agent (not Erasmus, not Vondel) assigned.
- **2026-03-23 (game-manual-review):** Reviewed `docs/game-manual.md` written by Erasmus. REJECTED. Found 5 critical factual errors verified against backend source: (1) FFA combat describes "advantage" (roll-twice) but code does 1d6+1 conditional on troop majority; (2) FFA ties go to DEFENDER (`>` not `>=`) but manual says attacker wins ties; (3) Fog of War is NOT optional — no `FogEnabled` flag exists in `GameDynamics`, always-on; (4) HQ claim freeze runs full 5 min regardless of recapture — code has no path to clear `ClaimFrozenUntil` early; (5) HQ hexes immune to direct combat (Commando Raid only) — manual says "recapture like any other hex" which is wrong. Two minor issues: enemy sighting memory UI max is 120s not 300s; Elimination tiebreaker framing misleading (should say alphabetical). Verdict written to `.squad/decisions/inbox/rembrandt-manual-review.md`.
- Team hired 2026-03-22 by Léon van de Broek
- Dutch Golden Age universe
- **2026-03-23 (beacon-redesign):** Led Scout beacon redesign architecture. Beacon changes from manual toggle to passive role trait (always-on when Scout has valid GPS). Share Intel becomes explicit 60s-cooldown ability for broadcasting beacon intel to alliance. Auto-activation hook: `GameplayService.UpdatePlayerLocation` (right place because beacon already requires location, and existing code maintains beacon fields when `IsBeacon` is true). Guards on manual toggle prevent Scouts from deactivating beacon. Server-side cone computation via `VisibilityService.ComputeBeaconSectorKeys` eliminates client drift. All role-specific logic gated on `PlayerRolesEnabled`; non-role games fully backward compatible. De Ruyter implemented backend changes (dotnet build + test ✅), Vermeer implemented frontend (npm build ✅) with concurrent rendering fixes (invisible cone tiles, `?` badge, instant reveal UX, pixel radius corrections). Merged decision #23 into `.squad/decisions.md`.
- **2026-03-23 (visibility-rearchitecture):** Investigated visibility architecture end-to-end. Key findings: (1) Backend does NOT strip raw tile data from Hidden cells — only stamps `VisibilityTier`. Frontend already has all data needed. (2) `BroadcastState` fires on EVERY hex change due to `movedToDifferentHex` check + Shepherd `LastVisitedAt` setting `gridChanged=true`. (3) This means per-player visibility recomputation (O(N×M)) runs on every movement. (4) The beacon fix already proved client-side visibility derivation works (`computeBeaconCone` in `beaconCone.ts`). Architecture decision: generalize the beacon pattern — frontend derives visibility locally from allied positions + owned territory + grid data. Backend reduces unnecessary full broadcasts. Deployed decision #25 into `.squad/decisions.md`. Coordinated De Ruyter (backend optimization) and Vermeer (frontend visibility derivation).
- **2026-03-24 (abilities-expansion-blueprint):** Produced full implementation blueprint for four ability changes: (1) Commando Raid — target changes to player's CURRENT hex; `ActivateCommandoRaid` loses both coord params; `ResolveRaidTarget` becomes a trivial current-hex stub for backward compat. (2) Tactical Strike — change `HexDistance > 1` check to `HexDistance != 1`; remove "Use Current Hex" frontend fallback. (3) Troop Transfer (NEW) — role-agnostic; bearing-based ally targeting with 45° cone tolerance; initiator/recipient SignalR events; `TroopTransferCooldownUntil` on `PlayerDto`; `ActiveTroopTransfers` on `GameState`. (4) Field Battle (NEW) — role-agnostic; neutral tile with enemy troops; `FieldBattleResolutionMode` enum (4 variants) in `GameDynamics`; 30-second join window via `Task.Run` + captured `IHubContext<GameHub>`; `battle.Resolved` flag prevents race between join and timer. Key arch decisions: Tactical Strike confirm required (no auto-fire); Troop Transfer shows name preview + confirm; Field Battle join via notification banner (not modal); no-join outcome = cancellation not win. Blueprint: `.squad/decisions/inbox/rembrandt-abilities-expansion-blueprint.md`. Decisions: `.squad/decisions/inbox/rembrandt-abilities-arch-decision.md`.

- 2026-03-24: Produced implementation blueprint for abilities expansion (Commando Raid target simplification, Tactical Strike adjacency constraint, Troop Transfer system, Field Battle system). Blueprint includes 21 detailed implementation steps spanning backend models, services, hubs, and frontend components. Created architectural decision document covering 5 binding decisions and 3 explicit non-decisions. De Ruyter and Vermeer assigned for concurrent backend/frontend implementation. Blueprint file: `.squad/decisions/inbox/rembrandt-abilities-expansion-blueprint.md` (49.6 KB).
- 2026-03-24: Abilities expansion sprint complete. Rembrandt (blueprint) → De Ruyter (backend) → Vermeer (frontend) → orchestration logs. All three agents completed work: Commando Raid simplified to current hex, Tactical Strike tightened to adjacent-only, Troop Transfer added with bearing-based targeting + 45s cooldown, Field Battle added with 30s join window + configurable resolution modes. Backend: `dotnet test` 299 total, 298 passed, 1 skipped. Frontend: `npm run lint` + `npm run build` both clean. See `.squad/orchestration-log/{timestamp}-{agent}.md` for details.


## Learnings

- **2026-03-30 (playtester-ux-plan):** Created comprehensive UX improvement plan based on first real-world playtest (14 scouts, 2 alliances). Key architectural findings:
  1. **Pickup triple-bug confirmed:** (a) `HexService.IsPlayerInHex` uses exact boundary match — GPS jitter causes ~50% rejection at hex edges; (b) `tileInteraction.ts` shows pickup on allied tiles but `GameplayService.cs:248` rejects it; (c) single-source pickup constraint returns `success: true` with no state change (decision #12).
  2. **Alliance name prop plumbed but never rendered:** `PlayerHUD.tsx:35` accepts `myAllianceName`, `PlayingHud.tsx:877` passes it, but it's never displayed.
  3. **Player marker position is SVG `transform`-based with no transition:** `PlayerLayer.tsx:462` uses `<g transform="translate(x,y)">` that snaps discretely. CSS transition on the `<g>` element would smooth it.
  4. **No color legend anywhere:** Neither `MiniMap.tsx` nor top-bar shows alliance→color mapping.
  5. **Reprioritized "team splash" from P3→P1** and "progressive HUD" from P2→P3 (depends on knowing what to hide, which requires completing simpler items first).
  Key file paths: `HexService.cs:239` (IsPlayerInHex), `tileInteraction.ts:244-260` (allied pickup UI), `PlayerLayer.tsx:456-510` (marker rendering), `PlayingHud.tsx:500-550` (top status bar), `PlayerHUD.tsx:35` (alliance name prop). Plan: `.squad/decisions/inbox/rembrandt-playtester-ux-plan.md`.

- **2026-05-15 (memory-audit-second-pass):** Forensic audit confirmed the backend write-side gap from decision #35 was NEVER fixed. De Ruyter only implemented the read-side TTL fix in `BuildStateForViewer` (`VisibilityService.cs`). The critical missing piece: `BroadcastPlayersPerViewer` (`VisibilityBroadcastHelper.cs` lines 62–84) calls `CreatePlayersForViewer` which computes `visibleHexKeys` but never calls `UpdateMemory`. Result: `RememberedHexes` is never stamped on movement-only broadcasts (`gridChanged = false`). Frontend safety net in `tricorderTileState.ts` (`serverTier` upgrade at line ~150) is correctly coded but permanently inert because it checks `cell?.lastSeenAt` which is only set by `ApplyRememberedCell` — which only fires when `RememberedHexes` has an entry — which never happens without `UpdateMemory`. Fix is one targeted change: add `UpdateMemory` call inside the `BroadcastPlayersPerViewer` loop in `VisibilityBroadcastHelper.cs`, computing `visibleHexKeys` once per viewer and reusing them for both the memory write and the player list filter. Prescription: `.squad/decisions/inbox/rembrandt-memory-audit.md`. Frontend changes not required.

- **2026-05-15 (visibility-fix-diagnosis):** Diagnosed adjacent-enemy-tile reveal bug introduced by `PlayersMoved` optimisation. Root cause: `getStrengthUnknownState` in `tricorderTileState.ts` checks `cell.visibilityTier` (raw server-stamped value from last `StateUpdated`) instead of the locally-derived `visibilityTierEarly`. After `PlayersMoved`, `alliedPlayerHexKeys` and `isLocallyVisible` correctly promote the tile to `'Visible'`, but `strengthUnknown` bypasses this because it never received the overridden tier. The backend is correct — `BuildStateForViewer` stamps `VisibilityTier.Hidden` but does NOT zero `troops`; the raw data is already in the store. Fix: pass `visibilityTierEarly` into `getStrengthUnknownState` instead of relying on `cell.visibilityTier`. Single-function, single-call-site change in `tricorderTileState.ts`. Fix spec written to `.squad/decisions/inbox/rembrandt-visibility-fix-spec.md`.

- **2026-03-25 (platform-analysis):** Led six-domain consolidated analysis. Key architectural findings:
  1. **Rate limiter critical bug**: `Program.cs` configures 60 req/s (3600/min) instead of 10 req/min — brute-force unprotected. Confirmed by De Ruyter, Grotius, Huygens. One-line fix.
  2. **EF Core shadow FK bug**: `GlobalHex.Owner` always null. `OwnerUserId` column not wired to navigation; EF uses shadow `OwnerId`. Fix: `.HasForeignKey(h => h.OwnerUserId)` + migration.
  3. **Database is SQL Server, not PostgreSQL**: Root `.env` and multiple docs reference PostgreSQL incorrectly. Actual stack: SQL Server 2022 throughout.
  4. **Service concentration risk**: `GameplayService` (1440 LOC) + `AbilityService` (1331 LOC) violate SRP; need splitting into role-specific services with `IRoleAbilityService` interface.
  5. **Frontend concentration risk**: `App.tsx` (721 LOC, 26 store reads) + `PlayingHud.tsx` (1018 LOC, 74-prop interface) need decomposition. `useSignalRHandlers` recreates 28 functions per `StateUpdated` due to `gameState` memo dep.
  6. **GameHub zero test coverage at 2160 LOC**: Confirmed by both De Ruyter and Spinoza. Highest regression risk in the codebase.
  7. **No Azure SignalR Service in Bicep**: Horizontal scale-out is impossible without it. Must provision before any App Service scaling.
  8. **No JWT revocation or account lockout**: Combined with misconfigured rate limiter, login brute-forcing is currently unconstrained.
  9. **Test suite strengths**: 276 deterministic unit tests with excellent `GameStateBuilder` infrastructure. Strong coverage for core services. Gaps in `DerivedMapStateService`, `GlobalMapService`, visibility service non-beacon paths, and all cancellation ability flows.
  10. **No frontend unit test runner**: Vitest setup is a 30-min investment that unlocks pure-function coverage for store reducers and tile interaction logic.
  Full analysis: `docs/analysis/platform-analysis-2026-03-25.md`. Decisions inbox: `.squad/decisions/inbox/rembrandt-platform-analysis-findings.md`.

- **2026-03-25 (pluggable-abilities-arch):** Designed pluggable abilities & roles architecture. Backend: `AbilityService` (1331 LOC monolith) splits into `CommanderAbilityService`, `ScoutAbilityService`, `EngineerAbilityService`, `SharedAbilityService`, all inheriting `RoleAbilityServiceBase` (abstract class with shared helpers). New `RoleProgressService` singleton extracted from `GameplayService` for fort/sabotage/demolish movement-tick progress tracking. `GameService` facade uses explicit named delegation (no runtime role→service dictionary). `GameHub.Gameplay.cs` unchanged. Frontend: `abilityRegistry` maps `AbilityKey` → `{ Card, roles, hubMethod, titleKey, mapFocusPreset }`. `AbilityCardProps = { myUserId, invoke }` — cards call hub methods directly via invoke. `PlayingHud` 13-branch if/else collapses to single registry lookup. `useGameActionsAbilities` 19x try/catch collapses to `makeHandler` factory. Spec: `.squad/analysis/rembrandt-pluggable-arch-spec.md`. Decisions: `.squad/decisions/inbox/rembrandt-pluggable-abilities-decisions.md`.

- **2026-05-15 (enemy-memory-design):** Diagnosed and designed the enemy tile sighting memory persistence bug end-to-end. Root cause is a two-layer failure: (1) Backend — `BroadcastPlayersPerViewer` (movement-only path in `VisibilityBroadcastHelper.cs`) calls `ComputeVisibleHexKeys` but never calls `UpdateMemory`, so enemy hex sightings via movement are never written to `PlayerVisibilityMemory.RememberedHexes`; (2) Frontend — when `isLocallyVisible` flips false after `PlayersMoved`, `visibilityTierEarly` falls back to `serverTier` which still holds the pre-approach `Hidden` value from the last `StateUpdated`, causing an immediate Hidden snap with no Remembered phase. Additionally, `STALENESS_FADING_THRESHOLD_MS` is hardcoded at 120s (tricorderTileState.ts line 519) and ignores `GameDynamics.EnemySightingMemorySeconds` (backend model: `GameState.cs:81`; frontend type: `types/game.ts:19`; configured 0–300s via `GameConfigService.SetEnemySightingMemory`). Fix: (1) De Ruyter adds `UpdateMemory` call inside `BroadcastPlayersPerViewer` loop after computing visibleHexKeys; (2) Vermeer adds a `localSeenAtRef` in `HexTile.tsx` to track when hostile tiles were last locally visible, passes it to `deriveTileState` as `localSeenAtMs`, and uses it to bridge the PlayersMoved→StateUpdated gap; (3) Vermeer replaces hardcoded staleness threshold with `(enemySightingMemorySeconds / 2) * 1000` (first half of memory window = fading, second half = stale). Design: `.squad/decisions/inbox/rembrandt-enemy-memory-design.md`.

- **2026-03-26 (enemy-memory-coordination):** Coordinated enemy tile sighting memory bug diagnosis and fix across backend and frontend. Diagnosed two-layer root cause: (1) Backend `BroadcastPlayersPerViewer` (movement-only path) never calls `UpdateMemory`, so hex sightings via movement aren't recorded to `RememberedHexes`; (2) Frontend `visibilityTierEarly` falls back to pre-approach `serverTier` (Hidden) when locally-visible flips false post-`PlayersMoved`, creating instant Hidden snap with no Remembered phase; + hardcoded 120s staleness threshold ignores `EnemySightingMemorySeconds` config. Delegated De Ruyter (backend TTL enforcement in projection) and Vermeer (local sighting memory + dynamic threshold). All three completed simultaneously. Orchestration logs: `.squad/orchestration-log/2026-03-26T09:41:37Z-{agent}.md` for each agent. Merged decision #35 into `.squad/decisions.md`.

- **2026-03-27 (enemy-memory-scrutiny):** Re-investigated enemy tile memory end-to-end after reported failure. CONFIRMED De Ruyter's fix from decision #35 was incomplete. De Ruyter correctly added TTL expiry logic in `BuildStateForViewer` (`VisibilityService.cs`) but MISSED the primary fix: `BroadcastPlayersPerViewer` (`VisibilityBroadcastHelper.cs` lines 62–78) still never calls `UpdateMemory`. The movement-only path therefore never writes to `PlayerVisibilityMemory.RememberedHexes`, making `BuildStateForViewer`'s projection logic irrelevant (no entries to project). Vermeer's safety net in `tricorderTileState.ts` (lines 131–134) relies on `cell.lastSeenAt` which is only set by `ApplyRememberedCell` — only populated when the backend has previously sent a `Remembered` tile. Since the backend never does (because memory was never written), the condition is always falsy. Fix: De Ruyter must add `UpdateMemory` call inside `BroadcastPlayersPerViewer` loop (one-loop refactor to precompute `visibleHexKeys` and share with `CreatePlayersForViewer`). Vermeer's fix is passively correct once backend ships — the stale `serverTierRaw = 'Visible'` from the last `StateUpdated` bridges the `PlayersMoved` timing gap. Diagnosis written to `.squad/decisions/inbox/rembrandt-memory-scrutiny-diagnosis.md`.

- **2026-05-15 (fieldbattle-independent-trigger-arch):** Architected the FieldBattle independent trigger feature. Key finding: the backend is **mostly complete** from prior sprints — `TryTriggerFieldBattle` (unified), `UpdatePlayerPosition` service method, and the hub handler all exist and are correct. The real gaps are: (1) Frontend — `useGameActionsGameplay.ts` never calls `invoke('UpdatePlayerPosition', ...)` from any GPS path; only `PlaceTroops` (button press) can trigger FieldBattle today. Fix: add a `useEffect` on `currentLocation` with a `prevPositionHexRef` guard that calls `UpdatePlayerPosition(q, r, lat, lng)` when the locally-computed hex changes. (2) Hub `UpdatePlayerPosition` is missing auto-join logic for a second enemy arriving at a hex that already has an active battle — `TryTriggerFieldBattle` returns null for them, they receive no invite. Fix: after `autoTriggeredBattle == null`, check for existing unresolved battle at (q, r) and send `FieldBattleInvite` directly to caller if they're a hostile player with troops. (3) `UpdatePlayerPosition` hub reads live mutable state for `BroadcastState` outside the lock — thread-safety bug. Fix: service method must return a snapshot from inside the lock. Dispatch logic is also triplicated across `PlaceTroops`, `UpdatePlayerPosition`, and `InitiateFieldBattle` hub methods; should be extracted to `DispatchFieldBattleInvitesAndScheduleResolution` helper. Architecture: `.squad/decisions/inbox/rembrandt-fieldbattle-independent-trigger-arch.md`.

- **2026-05-15 (fieldbattle-independent-trigger-correction):** Léon corrected the architecture. The right hook is **`UpdatePlayerLocation`** (existing GPS method), not `UpdatePlayerPosition`. `UpdatePlayerLocation` already fires on every move, already computes hex server-side, already returns `playerHexChanged`. Fix: add FieldBattle check inside `GameplayService.UpdatePlayerLocation` when `playerHexChanged == true`, return `ActiveFieldBattle?` in the tuple, and dispatch `FieldBattleInvite` in the hub handler. No new methods, no new hub endpoints. De Ruyter implementing directly. The architecture document in inbox (`rembrandt-fieldbattle-independent-trigger-arch.md`) over-engineered the solution and should be disregarded.

- **2026-03-29 (bug-hunt-round-4):** Led Bug Hunt Round 4 — 30-player/3-alliance scalability validation. Three-agent investigation: De Ruyter (backend threading/broadcast patterns), Vermeer (frontend rendering/state management), Spinoza (win conditions/lobby capacity at scale). **VERDICT: Production-ready. Zero critical bugs found.** Key findings: (1) Thread safety correctly implemented — all mutations protected by `lock(room.SyncRoot)` across all services; plain collections (List, Dictionary) safe because lock protects all access. (2) Performance bounded by fog-of-war architectural costs: per-viewer visibility calculation (30 computations/broadcast), full state serialization (30 × 150KB), grid iteration (O(grid_size) acceptable for radius-8 to radius-10). (3) Spinoza added 9 new large-scale tests in `LargeScaleGameTests.cs` — 368/369 tests pass (1 pre-existing skip). Win condition evaluation: <2ms for 721 hexes. StartGame: all 30 players positioned correctly. 30-player hard cap validated. (4) Identified 8 backend + 5 frontend performance risks — all acceptable trade-offs, not blockers. Recommended limits: 30 players max, radius-8 to radius-10 grid (217-331 hexes), 2 Hz update rate per player (500ms rate limit enforced). Architecture decisions: 30-player mode approved for production; performance optimization backlog is LOW priority (nice-to-have enhancements); grid size default remains radius-8; document simultaneous threshold tie-breaking behavior. Synthesis: `.squad/decisions/inbox/rembrandt-r4-synthesis.md`. Committed: `feat(qa): Bug Hunt Round 4 — 30-player scalability validation + 9 new tests` (5e3fa6c).
