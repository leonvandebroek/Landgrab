# De Ruyter — History

## Core Context
Backend Dev on Landgrab. ASP.NET Core 8, SignalR, EF Core, SQL Server/PostgreSQL. All game state is in-memory (singletons). GameService is the main façade. Hub split into 4 partial classes.

Key patterns:
- Endpoint extension methods on WebApplication
- JWT: ClaimTypes.NameIdentifier + JwtRegisteredClaimNames.Sub both present
- BCrypt factor 12, password min 8 chars
- Rate limiting "auth" policy on /api/auth/*
- AllianceDto (transient) ≠ Alliance EF entity (persistent)

## Learnings
- Team hired 2026-03-22 by Léon van de Broek
- 2026-03-22: Setup wizard location gating can get stuck due to frontend-side race (SetWizardStep(1) sent optimistically before SetMapLocation state update lands). Backend accepted manual coordinates already (lat/lng only), but wizard progression depended on subsequent state timing. Added backend guard in MapAreaService.SetMapLocation: when map location is set while CurrentWizardStep == 0, auto-advance to step 1 in the same authoritative state snapshot. This keeps manual coordinate flow and GPS flow both deterministic without changing SignalR message shape.
- **2026-03-22 (steen-continued-ux cross-reference):** Wizard fix was validated in 6-player playtest, but downstream gameplay reveals 4 critical/major blockers that require follow-up: null currentHex on game start, no debug movement fallback, false-success action feedback, no in-game location recovery. See .squad/decisions.md items 4–6.
- 2026-03-22: Fixed null `currentHex` at game start by initializing each player's position during `LobbyService.StartGame`. Spawn selection is deterministic and uses priority order: player-owned tile → alliance-owned tile → master tile → nearest grid fallback; this sets `CurrentLat/CurrentLng` and `CurrentHexQ/CurrentHexR` before entering Playing so location-gated actions no longer fail on first interaction. Kept SignalR payload shape unchanged and preserved existing mobile GPS + desktop keyboard update paths.
- 2026-03-22: Fixed manual HQ assignment consistency and validation in `AllianceConfigService.SetAllianceHQ`. The service now enforces alliance-ownership for the selected HQ tile, then appends an `AllianceHQAssigned` event **after** state assignment using the same `(q,r)` values written to `alliance.HQHexQ/HQHexR`, removing event/state mismatch risk for manual HQ placement. Added tests for success event payload and non-owned tile rejection.
- 2026-03-22: Investigated Q/E heading behavior from keyboard playtest. Backend hub methods that consume heading (`ActivateBeacon`, `ResolveRaidTarget`, `ResolveTacticalStrikeTarget`, `AttemptIntercept`) exist and validate heading; issue is frontend interaction gating: Q/E rotation handler exists in `GameMap.tsx` but only runs when compass rotation mode is enabled via the compass button.
- 2026-03-22: Investigated Dutch locale event log issue. Backend emits mostly hardcoded English `GameEventLogEntry.Message` strings across services. Frontend localizes only known `event.type` values and falls back to raw server `message` for unknown types, so many entries remain English in Dutch UI. Recommended direction: adopt typed event-key contract and localize on frontend; keep server `Message` as optional diagnostic fallback only. See .squad/decisions.md item 16–19.
- 2026-03-22: Beacon intel sharing now reuses a single backend sector computation path. Extracted `VisibilityService.ComputeBeaconSectorKeys(GameState state, PlayerDto player)` and consumed it from both fog-of-war visibility and new `AbilityService.ShareBeaconIntel(roomCode, userId)`, preventing drift between what a scout sees and what alliance intel-sharing snapshots persist.
- 2026-03-22: Reworked `VisibilityService` intel model to make beacon sector visibility personal-only and preserve Scout-gated sharing through explicit `ShareBeaconIntel`. `ComputeVisibleHexKeys` now adds beacon sector keys only for the viewing player when `IsBeacon` is true, and adds always-fresh alliance-border enemy tiles by scanning neighbors of alliance-owned hexes. `UpdateMemory` now writes all visible hostile intel to viewer memory but auto-shares only `visibleHostileKeys - beaconSectorKeys` to allied memories, so beacon-derived intel is never passively broadcast while border/proximity intel still is. (Cross-ref: vondel-intel-design produced 5 design options, Eyes of the Scout selected; de-ruyter-visibility implemented with tests: 289 total, 288 passed, 1 skipped.)
- 2026-03-22: Fixed beacon reveal pipeline so active beacons re-scan on every `UpdatePlayerLocation` (movement and heading changes) and publish scan geometry for UI overlays. Root cause: beacon sector intel only lived in computed visibility during full `StateUpdated`, but movement updates often sent `PlayersMoved` only, so no fresh projection/memory update happened while beacon moved/rotated. Added `PlayerDto.BeaconScanHexes` (grid keys) and now refresh `BeaconLat/BeaconLng/BeaconHeading/BeaconScanHexes` in `GameplayService.UpdatePlayerLocation` when `IsBeacon` is active, forcing `StateUpdated` broadcast via `gridChanged`. Also ensured snapshot cloning carries `HexCell.LastSeenAt` so remembered enemy intel staleness survives projection for Amber Archive.
- 2026-03-22: Adopted full-tile broadcast model for fog-of-war. `VisibilityService.BuildStateForViewer` no longer masks hidden cells; it only sets `VisibilityTier` while preserving tile gameplay fields. Removed backend-only beacon scan geometry (`PlayerDto.BeaconScanHexes`) and movement-triggered beacon re-scan/grid-change forcing. Added explicit share flow `ShareBeaconIntel(roomCode, hexKeys)` so scout clients send cone hex keys and backend refreshes `PlayerVisibilityMemory.RememberedHexes[*].SeenAt` for alliance members. Validation: `dotnet build` + `dotnet test` passing (293 total, 292 passed, 1 skipped).
- **2026-03-22 (beacon-architecture-simplification):** Completed architectural refactor with vermeer. Removed server-side masking (`VisibilityService.BuildStateForViewer` only sets `VisibilityTier`), removed implicit beacon re-scan on movement (`GameplayService.UpdatePlayerLocation` no longer forces `gridChanged` from beacon heading/position), deleted `PlayerDto.BeaconScanHexes` field. Backend now exposes hub method `ShareBeaconIntel(roomCode, hexKeys[])` that receives cone hex list from client and updates alliance `PlayerVisibilityMemory.RememberedHexes` with fresh `SeenAt` timestamps. Frontend computes cone geometry client-side via new `beaconCone.ts` utility and invokes ShareBeaconIntel explicitly. Rationale: aligns backend role (authoritative state) with display policy (client rendering). Eliminates redundant server cone computation. Clarifies scout teamwork as explicit action. Validation: `dotnet build` ✅, `dotnet test` ✅ (292/293, 1 skipped). See orchestration log 2026-03-22T17:39:41Z-de-ruyter.md.
- 2026-03-23: Redesigned Scout beacon backend flow so beacon is role-passive (always on for Scout when roles are enabled) and Share Intel is the active Scout ability with a 60s cooldown. Added `PlayerDto.ShareIntelCooldownUntil`, centralized scout beacon state sync via `GameStateCommon.SyncBeaconStateForRole(state, player)`, and updated role lifecycle paths (`LobbyService`, `RoomService`, `GameplayService` movement sync) so scout `IsBeacon/BeaconLat/BeaconLng/BeaconHeading` stay authoritative without requiring deactivate/activate toggles. `AbilityService.ShareBeaconIntel` now ignores client-supplied hex lists and computes the cone server-side through `VisibilityService.ComputeBeaconSectorKeys`, then writes remembered intel for alliance members with existing staleness semantics. Validation: `dotnet build --configuration Debug` (Landgrab.Api) ✅ and `dotnet test` (Landgrab.Tests) ✅ (295 total, 294 passed, 1 skipped).
- 2026-03-23: Fixed delayed proximity reveal after movement by updating `GameHub.UpdatePlayerLocation` to broadcast full `StateUpdated` whenever the mover changes hex (`PreviousHexKey` vs current key), not only when `gridChanged` is true. This forces immediate per-viewer visibility recomputation through `VisibilityBroadcastHelper.CreateStateForViewer` + `VisibilityService.ComputeVisibleHexKeys` so non-Scout 1-hex adjacency reveals appear instantly while Scout beacon behavior remains unchanged. See `.squad/orchestration-log/2026-03-23T13:14:04Z-de-ruyter.md` and `.squad/decisions.md`.
- 2026-03-23: Optimized movement broadcasts to eliminate unnecessary full-state recomputation. With frontend now deriving visibility locally, backend no longer broadcasts full `StateUpdated` on every hex boundary crossing. Changes: (1) `GameplayService.UpdatePlayerLocation` no longer sets `gridChanged = true` when updating Shepherd `LastVisitedAt` metadata — this value is server-side only for `TroopRegenerationService` decay logic and doesn't need immediate client broadcast. (2) `GameHub.UpdatePlayerLocation` now only calls `BroadcastState` (full per-viewer projection) when `gridChanged` is true (territory claims, forts, combat, phase changes). Simple position updates use lightweight `BroadcastPlayersPerViewer` instead. Removed unused `HasPlayerChangedHex` helper. Validation: `dotnet build` ✅, `dotnet test` ✅ (295 total, 294 passed, 1 skipped). Rationale: Reduces O(N×M) visibility recomputation on every movement; frontend computes visibility from player positions client-side. Orchestration log: `.squad/orchestration-log/2026-03-23T13:40:42Z-de-ruyter-visibility.md`.
- 2026-03-23 (game-manual-research): Performed comprehensive backend code analysis for player-facing game manual. Extracted precise, authoritative game mechanics data from 18+ service files and model definitions. Documented: (1) All 3 player roles (Commander, Scout, Engineer) with 9 total abilities including exact cooldowns, ranges, costs, and progression mechanics; (2) Combat resolution algorithms for all 3 combat modes (Classic deterministic, Balanced/Siege dice-based with 3 rounds and clamped hit probability 0.2–0.8); (3) Troop regeneration system (30s tick, +1 base, 3× presence bonus, decay/sabotage/drain blockers); (4) Fog of war visibility rules (1-hex radius default, Scout beacon 3-hex 45° cone, alliance intel sharing with staleness tracking); (5) Win condition formulas (TerritoryPercent default 60%, Elimination, TimedGame); (6) All game configuration parameters (grid radius 8, tile size 25m, starting troops 3, claim modes, HQ mechanics with 5-min freeze penalty). Research saved to `.squad/decisions/inbox/de-ruyter-game-manual-research.md` (28KB authoritative reference). All numeric values extracted from C# constants (`ShareIntelCooldown = 60s`, `BalancedCombatRounds = 3`, `VisibilityRadius = 1`, `BeaconRange = 3`, etc.). Cross-referenced AbilityService (918 lines), GameplayService (1435 lines), WinConditionService, LobbyService, VisibilityService, TroopRegenerationService, and 10+ model files. Output formatted for direct consumption by manual writers with validation rules, formulas, and edge case documentation.
- 2026-03-23 (manual-corrections-pass3): Applied final two corrections to `docs/game-manual.md` after code verification. **Error 1** (Commando Raid): Changed success condition from "≥50% of alliance members physically present" to "At least 2 alliance members must be standing in the target hex at deadline AND must outnumber any defenders present" per `GameplayService.cs:680` (`attackers.Count >= 2 && attackers.Count > defenders.Count`). **Error 2** (Demolish): Changed initiation step from "Stand adjacent to an enemy fort" to "Walk onto the enemy fort hex" per `AbilityService.cs:747` (`currentCell.IsFort` check on player's current hex, not adjacent). Both corrections verified against source code before application. Manual now passes Rembrandt's review gate.
- 2026-03-24 (abilities-expansion-backend): Implemented backend abilities expansion from Rembrandt blueprint. Commando Raid activation no longer accepts target coordinates and always targets the commander’s current hex (hub + facade + service), Tactical Strike now enforces strictly adjacent-only targets (`HexDistance == 1`) with updated error text, and added Troop Transfer + Field Battle systems end-to-end (new `GameState` models/enums/collections, `AbilityService` flows, `GameService` facades, `GameHub.Gameplay` methods/events, `GameHub.Host.SetFieldBattleResolutionMode`, `GameConfigService.SetFieldBattleResolutionMode`, and field-battle cooldown clearing in `GameplayService.UpdatePlayerLocation`). Added/updated tests in `AbilityServiceTests`; validation passed with `dotnet build --configuration Debug` and `dotnet test` (299 total, 298 passed, 1 skipped).

- 2026-03-24 (abilities-expansion-backend): Implemented backend abilities expansion from Rembrandt blueprint. Commando Raid target removed from signature and derives from current hex, Tactical Strike adjacency changed to strictly adjacent-only (HexDistance != 1), Troop Transfer system added end-to-end (models, AbilityService methods, hub methods, facades, cooldown lifecycle), Field Battle system added end-to-end (models, AbilityService methods with Task.Run async timer, hub methods, host config, cooldown lifecycle). Updated GameStateCommon.SnapshotState and GameHub.SanitizeGameDynamics for new fields. Added/updated tests in AbilityServiceTests. Validation passed: `dotnet build --configuration Debug` ✅, `dotnet test` ✅ (299 total, 298 passed, 1 skipped). See orchestration log `.squad/orchestration-log/2026-03-24T16:25:04Z-de-ruyter.md`.

- 2026-03-26: Fixed enemy remembered-hex persistence semantics in `VisibilityService.BuildStateForViewer`. Root cause was missing TTL enforcement for `PlayerVisibilityMemory.RememberedHexes`: remembered enemy tiles could bypass configured memory-window behavior and projection could fall back incorrectly. Added authoritative cutoff check using `state.Dynamics.EnemySightingMemorySeconds` (passed into projection), preserving `Remembered` only within window, purging expired entries, and purging immediately when memory is disabled (`0s`). Added regression tests in `VisibilityServiceTests` for within-window, expired-window, and disabled-memory cases. Validation passed: `dotnet build --configuration Debug` and `dotnet test` (343 total, 342 passed, 1 skipped).

- **2026-03-26 (enemy-memory-backend):** Implemented backend fix for enemy tile memory persistence. Root cause: `VisibilityService.BuildStateForViewer` applied remembered intel unconditionally without TTL enforcement against `GameDynamics.EnemySightingMemorySeconds`. Added authoritative cutoff check in projection: preserve `Remembered` tier only while `RememberedHex.SeenAt >= cutoff`; purge expired entries during projection; immediate hide + purge if memory disabled (0s). Added regression tests covering within-window remember, expired-window purge, and disabled-memory purge. Validation: `dotnet build --configuration Debug` ✅, `dotnet test` ✅ (343 total, 342 passed, 1 skipped). See orchestration log `.squad/orchestration-log/2026-03-26T09:41:37Z-de-ruyter.md` and decision #35 in `.squad/decisions.md`.
- 2026-03-26: Traced movement→visibility pipeline and confirmed write-side memory gap on movement-only updates. `VisibilityService.UpdateMemory` was only guaranteed through full `StateUpdated` projection (`VisibilityBroadcastHelper.CreateStateForViewer`), while `GameHub.UpdatePlayerLocation` could send `PlayersMoved` only when `gridChanged == false`. Implemented pre-move memory stamping inside `GameplayService.UpdatePlayerLocation` for Alliances (`ComputeVisibleHexKeys` + `UpdateMemory` before `SetPlayerLocation`), added `playerHexChanged` signal, and restored full `StateUpdated` broadcast on hex-boundary movement (`gridChanged || playerHexChanged`) so remember-tier projection runs immediately after crossing. Added regression test `UpdatePlayerLocation_WhenEnemyHexLeavesVisibility_StampsRememberedHexBeforeMove`; validation: `dotnet build --configuration Debug` ✅, `dotnet test` ✅ (348 total, 347 passed, 1 skipped).
- 2026-03-26 (enemy-memory-scrutiny-fix): Root-cause scrutiny found movement-only `PlayersMoved` broadcasts were not persisting hostile tile sightings into `room.VisibilityMemory`, so `Remembered` intel could be missing immediately after moving away. Implemented fix in `VisibilityBroadcastHelper.CreatePlayersForViewer` to call `visibilityService.UpdateMemory(...)` during movement projections, and added regression test `VisibilityBroadcastHelperTests.BroadcastPlayersPerViewer_WhenEnemyTileIsVisible_RefreshesRememberedHexMemory`. Verified backend build succeeds; targeted memory tests pass; full suite currently shows an unrelated pre-existing failure in `GameplayServiceTests.PlaceTroops_WhenCaptureMeetsWinCondition_EndsTheGame`.
- 2026-03-26: Updated backend enemy memory defaults to enforce non-zero behavior. `GameDynamics.EnemySightingMemorySeconds` now defaults to `120`, `GameHub.SanitizeGameDynamics` clamps incoming dynamics to minimum `15`, and `GameConfigService.SetEnemySightingMemory` now persists `Math.Max(15, seconds)` so "Off" (`0`) can no longer be set via API. Validation: `dotnet build --configuration Debug` ✅ and `dotnet test` ✅ (348 total, 347 passed, 1 skipped).
- 2026-03-26: Implemented auto-trigger for FieldBattle when two enemy players land on same neutral tile. Hook point: `GameplayService.PlaceTroops` line 345, checked BEFORE claiming neutral hex. Added helper method `CheckAndAutoTriggerFieldBattle` that guards against cooldown, active battles, Tactical Strike/Commando Raid hex conflicts, and checks for enemy presence with troops. Returns `ActiveFieldBattle` on trigger, null otherwise. Updated `PlaceTroops` signature to 5-tuple `(state, error, previousOwnerId, combatResult, autoTriggeredBattle)` and propagated through `GameService` facade and `GameHub.Gameplay.PlaceTroops`. Hub broadcasts `FieldBattleInvite` to enemies and schedules 30s auto-resolution with `FieldBattleResolved` event. Guard: Battle NOT triggered if one already pending for that hex. Validation: `dotnet build` ✅, `dotnet test` ✅ (348 total, 347 passed, 1 skipped). Key insight: Auto-trigger must happen BEFORE neutral claim, not after, because `InitiateFieldBattle` requires `cell.OwnerId == null` (neutral tile constraint).
- 2026-03-26: Fixed FieldBattle auto-trigger to also check enemy-owned tiles when owner is physically present. Root cause: The first neutral-tile implementation only triggered when BOTH players arrived before either claimed the hex — impossible in sequential gameplay. Added physical presence check at line 378-398 in the enemy-owned hex branch of `PlaceTroops`: before normal hex combat, check if any enemy player with troops is standing on the target tile using `TryGetCurrentHex(state, player, out q, out r)` which derives hex from GPS coords (CurrentLat/CurrentLng) or stored hex position (CurrentHexQ/CurrentHexR). If enemy present → call new helper `CheckAndAutoTriggerFieldBattleOnEnemyTile` (identical to neutral variant but without `cell.OwnerId == null` guard). FieldBattle semantics: two players physically on the same tile fighting directly, works whether tile is neutral OR enemy-owned-but-owner-present. Hub code already correctly sends `FieldBattleInvite` to all enemies on hex via `TryGetCurrentHex` check (lines 560-588 in GameHub.Gameplay.cs). Validation: `dotnet build` ✅ (warnings unrelated), `dotnet test` ✅ (348 total, 346 passed, 1 skipped, 1 pre-existing failure in PlaceTroops_WhenCaptureMeetsWinCondition_EndsTheGame).

- 2026-03-26 (fieldbattle-independent-trigger): Made FieldBattle trigger independently via GPS movement. Extracted common trigger logic into `TryTriggerFieldBattle(GameState state, PlayerDto attacker, int q, int r)` — public method covering all guards (carried troops, cooldown, active battle, tactical strike/raid conflict, existing battle on hex, enemy presence) without a neutral-tile constraint. `CheckAndAutoTriggerFieldBattle` and `CheckAndAutoTriggerFieldBattleOnEnemyTile` are now thin private wrappers delegating to it. `UpdatePlayerLocation` return type expanded to 5-tuple `(state, error, gridChanged, playerHexChanged, autoTriggeredBattle)`: when `playerHexChanged == true`, calls `TryTriggerFieldBattle` (neutral tile → neutral variant, else enemy-tile variant) and sets `gridChanged = true` on trigger. `GameService.UpdatePlayerLocation` updated to match 5-tuple. `GameHub.UpdatePlayerLocation` handler dispatches `FieldBattleInvite` to enemies and caller (`isInitiator=true`) when `autoTriggeredBattle != null`, with identical 30s auto-resolution Task.Run as PlaceTroops. No new methods added. Validation: `dotnet build --configuration Debug` ✅, `dotnet test` ✅ (348 total, 347 passed, 1 skipped).
- 2026-03-26 (fieldbattle-manual-only): Reverted incorrect FieldBattle auto-trigger from GPS movement path. `GameplayService.UpdatePlayerLocation` now returns to 4-tuple `(state, error, gridChanged, playerHexChanged)` and no longer calls `CheckAndAutoTriggerFieldBattle`/`CheckAndAutoTriggerFieldBattleOnEnemyTile`. Updated `GameService.UpdatePlayerLocation` signature to match and removed movement-triggered invite/timer dispatch block from `GameHub.UpdatePlayerLocation`. Manual `InitiateFieldBattle` flow remains unchanged as the only trigger path for FieldBattle initiation. Validation: `dotnet build --configuration Debug` ✅ and `dotnet test` ✅ (348 total, 347 passed, 1 skipped).
