# Orchestration Log: P2 Dialog Stacking Fix

**Timestamp:** 2026-03-22T14:16:02Z  
**Agent:** vermeer  
**Status:** ✅ Completed

## Summary

Implemented fix for dialog stacking issue where rapid combat and claim outcomes caused multiple outcome dialogs (`CombatModal`, `TroopDeployModal`) to render simultaneously, obscuring game state.

## Approach

**Option A — Queue:** Added `QueuedOutcomeDialog` discriminated union and `outcomeDialogQueue` array to `gameplayStore.ts`. Result setters now queue dialogs when one is already visible and auto-promote on dismissal.

## Changes

| File | Changes |
|------|---------|
| `gameplayStore.ts` | Added `QueuedOutcomeDialog` type, `outcomeDialogQueue` state, updated `setCombatResult` / `setNeutralClaimResult`, updated `clearGameplayUi` |
| `GameView.tsx` | No changes |
| `useSignalRHandlers.ts` | No changes |
| `agentBridge.ts` | No changes |

## Verification

- Build: ✅ `npm run build` passes
- Lint: ✅ `npm run lint` passes
- Backward compatibility: ✅ Full (no API changes)
- Testing: ✅ Requires e2e/playtester validation

## Notes

- Queue approach chosen over replace (Option B) to preserve all outcomes the player needs to see
- Queue approach simpler than consolidation (Option C) and requires no backend changes
- Single-file implementation reduces merge conflict risk
