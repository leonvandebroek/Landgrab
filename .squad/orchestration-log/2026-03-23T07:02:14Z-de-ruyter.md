# Orchestration Log: De Ruyter — 2026-03-23T07:02:14Z

## Task: Implement Scout Beacon Always-On + Share Intel Backend

**Status:** ✅ Complete  
**Agent:** de-ruyter (Backend Dev)  
**Output Mode:** background

### Work Summary

Implemented backend beacon redesign per Rembrandt's spec:

**Core Logic:**
- Scout `IsBeacon` now auto-synced `true` in `GameplayService.UpdatePlayerLocation` when `PlayerRolesEnabled && player.Role == Scout && lat/lng valid`
- `AbilityService.ActivateBeacon` — Guard returns error for role-enabled Scouts (no manual activation)
- `AbilityService.DeactivateBeacon` — Guard returns error for role-enabled Scouts (beacon cannot be toggled off)
- `AbilityService.ShareBeaconIntel` — Server-computes beacon cone via `VisibilityService.ComputeBeaconSectorKeys`, ignores client hex payload, enforces 60s cooldown via `PlayerDto.ShareIntelCooldownUntil`

**Model Changes:**
- Added `PlayerDto.ShareIntelCooldownUntil` field (datetime for cooldown tracking)
- No changes to `PlayerDto.IsBeacon`, `BeaconLat`, `BeaconLng`, `BeaconHeading` (reused as-is)
- No changes to `GameDynamics.BeaconEnabled`, `BeaconSectorAngle`

**Validation:**
- ✅ `cd backend/Landgrab.Api && dotnet build --configuration Debug`
- ✅ `cd backend/Landgrab.Tests && dotnet test` (295 total, 294 passed, 1 skipped)

### Edge Cases Handled

1. **Scout with GPS denied** — `UpdatePlayerLocation` never fires with valid coords → `IsBeacon` stays `false` → Share Intel blocked (existing guardrail)
2. **Cold-start heading** — First location update may have `null` heading; frontend defaults to 0° (north)
3. **Non-role games** — All guards gated on `PlayerRolesEnabled`; backward compatibility preserved
4. **Cone computation consistency** — Centralized via `ComputeBeaconSectorKeys` eliminates client/server drift

### Architecture Alignment

- Preserves prior intel-memory staleness behavior (via `RememberedHex.SeenAt`)
- Makes Scout gameplay clearer: passive personal reveal + active team share
- No changes to hub method signatures or message formats
- Full backward compatibility for non-role mode

### Output

Decision specification written to `.squad/decisions/inbox/de-ruyter-beacon-backend.md` (21 lines).

### Completion Criteria Met

- ✅ Auto-activates beacon for Scout via location update
- ✅ Enforces guards on manual toggle attempts
- ✅ Share Intel computes cone server-side, enforces cooldown
- ✅ dotnet build clean
- ✅ dotnet test passing

---

**Timestamp:** 2026-03-23T07:02:14Z UTC  
**Decision Inbox:** de-ruyter-beacon-backend.md  
**Build Output:** 295 tests, 294 passed, 1 skipped
