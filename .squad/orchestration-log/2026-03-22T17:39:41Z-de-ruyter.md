# De Ruyter Orchestration — 2026-03-22T17:39:41Z

## Task
Strip server-side tile masking and implement explicit beacon intel share mechanism.

## Spawn Command
```
cd backend/Landgrab.Api && dotnet run
cd backend/Landgrab.Tests && dotnet test
```

## Work Completed

### 1. Removed Server-Side Tile Masking
- `VisibilityService.BuildStateForViewer` now only sets `VisibilityTier` enum (`Visible`, `Remembered`, `Hidden`)
- Eliminated null/zeroing of ownership, troop, and other fields on hidden tiles
- Backend now always sends full tile data; frontend controls rendering policy via visibility tier

### 2. Removed Movement-Driven Beacon Re-Scan
- `GameplayService.UpdatePlayerLocation` still updates beacon position (`BeaconLat/BeaconLng`) while active
- Removed forced cone recomputation and `gridChanged` flag marking from movement/heading changes
- Beacon heading no longer auto-updated from player movement

### 3. Removed PlayerDto.BeaconScanHexes Field
- Deleted from model definition
- Removed from cloning/projection paths across `GameStateCommon`, `VisibilityBroadcastHelper`

### 4. Implemented Explicit Beacon Intel Share
- New hub method: `ShareBeaconIntel(string roomCode, string[] hexKeys)`
- Validates room existence, payload shape, active beacon, authorization
- Updates `PlayerVisibilityMemory.RememberedHexes` with fresh `SeenAt` timestamp and current tile snapshot data for alliance members
- Broadcasts `StateUpdated` event to synchronize shared intel across team

## Validation

| Check | Result |
|-------|--------|
| `dotnet build --configuration Debug` | ✅ Clean |
| `dotnet test` | ✅ 292/293 passed (1 skipped) |

## Rationale

Aligns backend architecture with decision: client-side display policy, server-side authoritative state. Removes unnecessary backend cone bookkeeping for movement. Preserves scout teamwork mechanic via explicit "Share Intel" action that updates remembered tile freshness timestamps through alliance history.

## Files Modified

- `backend/Landgrab.Api/Services/VisibilityService.cs` (removed masking)
- `backend/Landgrab.Api/Services/GameplayService.cs` (removed re-scan)
- `backend/Landgrab.Api/Services/GameStateCommon.cs` (removed BeaconScanHexes)
- `backend/Landgrab.Api/Services/AbilityService.cs` (removed beacon share payload)
- `backend/Landgrab.Api/Hubs/GameHub.cs` (added ShareBeaconIntel method)
- Test support files updated

---

**Status:** Ready for frontend consumption
